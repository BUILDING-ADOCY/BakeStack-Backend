node_modules
dist
.git
.gitignore
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage
.turbo
.claude

# Secrets — provided at runtime via compose env_file/environment, never baked in
.env
.env.*
!.env.example

# Not needed inside the image
test
*.md
.github
Dockerfile
.dockerignore
docker-compose*.yml
